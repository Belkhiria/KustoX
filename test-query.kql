// Test query for StormEvents data
StormEvents
| take 10

// User's specific query that should return aggregated data
StormEvents
| take 1
| project StartTime, EndTime, EventType, State, DamageProperty
| summarize TotalEvents = count(), TotalDamage = sum(DamageProperty) by EventType, State

// Alternative query to test table existence
StormEvents
| count

// Another test query to see table structure
StormEvents
| getschema
| take 10

// Simple test to ensure basic functionality
print "Test query working"
